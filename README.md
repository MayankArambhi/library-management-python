# 📚 Library Management System (Python + CSV)

A simple command-line library management system built in Python using CSV
for persistent storage.

# Features
- Add books with ISBN codes
- Borrow books
- Return books
- View all books
- Excel-safe ISBN handling (prevents auto-formatting issues)

# Tech Used
- Python
- CSV file handling
- Pandas (for viewing data)
- Sleep time(for smooth workflow)